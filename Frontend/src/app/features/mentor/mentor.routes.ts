import { Routes } from '@angular/router';

/**
 * Mentor routes - Protected by mentorGuard at parent level in app.routes.ts
 *
 * Future routes to implement:
 * - dashboard: Mentor dashboard
 * - profile: Mentor profile view
 * - sessions: Session management
 * - bookings: Booking requests
 * - availability: Availability calendar
 * - earnings: Earnings/payments
 * - reviews: Reviews received
 * - settings: Mentor settings
 */
export const MENTOR_ROUTES: Routes = [
  // TODO: Add mentor routes here as components are created
  // Example:
  // {
  //   path: 'dashboard',
  //   loadComponent: () => import('./dashboard/dashboard.component').then(m => m.DashboardComponent)
  // },
];
